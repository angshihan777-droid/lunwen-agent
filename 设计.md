# 论文研究 Agent — 设计文档

## 一、项目定位

不是聊天机器人，是能主动执行动作的研究助手。
用户上传论文后，Agent 自动调用工具链完成提取、对比、检索等任务，而不只是被动回答问题。

---

## 二、核心功能

| 编号 | 功能 | 用户操作 | Agent 输出 |
|------|------|---------|-----------|
| F1 | PDF 上传与解析 | 拖拽或选择 PDF | "上传成功，已解析 XX 页" |
| F2 | 结构化信息提取 | 点击"提取关键信息" | 6字段卡片：研究问题 / 方法 / 数据集 / 指标 / 结论 / 局限性 |
| F3 | 快速摘要生成 | 点击"生成摘要" | 200-300 字可读摘要 |
| F4 | 多轮对话问答 | 在对话框提问 | 回答并记住对话上下文 |
| F5 | 多篇论文对比 | 选多篇论文点"对比" | Markdown 表格（横轴论文，纵轴维度） |
| F6 | 研究缺口识别 | 选多篇论文点"找缺口" | 分析哪些问题尚未被解决 |

### 上下文管理（两层）

- **对话历史层**：ConversationBufferWindowMemory(k=10)，保留最近 10 轮，兼容所有中转站模型
- **长文档层**：RecursiveCharacterTextSplitter + FAISS，论文超长时按块检索，不报 token 超限

### 历史对话（前端本地）

- 每条对话保存在 `localStorage("conv_history")`，最多 30 条
- 对话标题自动取第一条用户消息前 22 字
- 左侧「历史对话」面板可切换查看任意历史；「✨ 新对话」按钮保存当前对话并重置状态

---

## 三、工具链（8 个 LangChain Tools）

### 本地处理类（处理已上传论文）

| Tool | 作用 |
|------|------|
| `extract_tool` | 结构化提取论文 6 个核心字段 |
| `summary_tool` | 生成可读摘要 |
| `compare_tool` | 对比多篇论文，输出 Markdown 表格 |
| `search_tool` | RAG 检索——在论文内容中语义搜索 |
| `gap_tool` | 识别多篇论文的研究缺口 |
| `citation_tool` | 提取参考文献列表，尝试查询每篇引用摘要 |

### 外部获取类（联网主动获取信息）

| Tool | 作用 |
|------|------|
| `arxiv_tool` | 根据关键词/标题去 ArXiv 搜索相关论文 |
| `web_search_tool` | 搜索作者近期工作、引用讨论（使用 Tavily API） |

---

## 四、API Key 管理设计（多配置版）

用户在前端设置面板填入自己的 API Key，无需改代码，无需部署环境变量。

**设置面板字段：**
- 已保存配置下拉（选择即切换，立即生效）
- LLM 提供商选择：OpenAI / DeepSeek / 中转站 / Anthropic
- LLM API Key
- Base URL（中转站时显示）
- 模型（手动输入 or 拉取列表）
- Tavily API Key（可选）
- 「保存」按钮

**存储方式：**
- 服务器端：Key 存在当前会话的内存 dict（`config._sessions_config`），不写数据库，重启清空
- 浏览器端：每次点「保存」，以 `"provider · model"` 为名自动存入 `localStorage`，刷新不丢失
- 选择已保存配置 → 自动填充表单并立即发送到服务器生效，无需再点保存

**多用户隔离：**
- 每个浏览器 Tab 在 localStorage 生成唯一 `session_id`，所有请求携带 `X-Session-ID` 头
- 后端用 `contextvars.ContextVar` 路由：每个会话有独立的配置、对话记忆、论文存储

---

## 五、我喜欢的设计

- 结构化输出（Pydantic 定义 schema，不是自由文本）
- Agent 主动执行，工具链驱动，不是被动问答
- 论文对比输出 Markdown 表格，清晰直观
- API 设计干净，每个功能对应独立 endpoint
- Prompt 分离——不同任务用不同 Prompt Template，不混用
- 对话可感知上下文，Agent 记得之前问过什么
- 前端设置面板支持用户自己输入 API Key

---

## 六、我不喜欢的设计

- 纯聊天框体验（看起来像普通 ChatGPT wrapper）
- 输出无结构，大段自由文本
- 前端过度复杂（React/Next.js 等，不是核心价值）
- 云端数据库依赖（增加部署难度）
- 把所有功能塞进一个 prompt（应该用 Tools 拆分）
- 重复使用 LangGraph（已有项目）
- API Key 硬编码在代码里

---

## 七、用户典型使用路径

**场景一：快速读懂一篇新论文**
1. 在设置面板填入 API Key → 保存
2. 上传 PDF
3. 点"提取关键信息" → 拿到结构化卡片
4. 在对话框追问细节，Agent 记得刚才的内容

**场景二：写综述 / 做调研**
1. 批量上传 3-5 篇相关论文
2. 点"论文对比" → 得到对比表格
3. 点"研究缺口" → 得到 gap 分析
4. 用 ArXiv 工具继续搜索相关文献

---

## 八、部署方案

- **后端**：Railway（自动识别 Python，push 代码自动部署）
- **域名 + HTTPS**：Cloudflare（免费 DNS + 自动 HTTPS）
- **向量库**：FAISS 本地，随应用一起运行，无需额外服务
