# 论文研究 Agent — 开发文档

## 一、使用的技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| LLM 接入 | `langchain-openai` / `langchain-anthropic` | 多模型支持，统一接口，可切换 |
| Agent 核心 | `langchain` AgentExecutor + Tool | 工具调度主逻辑 |
| Prompt | `PromptTemplate`, `ChatPromptTemplate` | 不同任务分离 prompt，不混用 |
| 记忆管理 | `ConversationBufferWindowMemory(k=10)` | 滑动窗口保留最近 10 轮，兼容所有中转站模型；前端 localStorage 保存完整历史供回看 |
| RAG 检索 | `RecursiveCharacterTextSplitter` + `FAISS` + `RetrievalQA` | 长论文 token 管理 |
| 结构化输出 | `Pydantic` + `PydanticOutputParser` | 强约束输出格式 |
| PDF 解析 | `PyMuPDF (fitz)` | 提取论文文本 |
| ArXiv 搜索 | `arxiv` Python 库 | 搜索相关论文（免费） |
| 网络搜索 | `langchain-community` TavilySearchResults | 搜索引擎集成（需 Tavily API Key） |
| 后端 | `FastAPI` + `uvicorn` | API 服务，异步支持 |
| 前端 | 原生 HTML + Tailwind CSS + Fetch API | 轻量，不引入前端框架 |
| 依赖管理 | `pip` + `requirements.txt` | 简单直接 |
| 环境变量 | `.env` + `python-dotenv` | 本地开发时加载默认配置 |

---

## 二、不使用的技术（及原因）

| 技术 | 不用的原因 |
|------|-----------|
| LangGraph | 简历已有同类项目，避免重复展示 |
| PostgreSQL / SQLite | 无持久化需求，内存存储足够 |
| Docker | 增加复杂度，Railway 直接部署 Python 无需容器 |
| Pinecone / Weaviate | 云端收费，FAISS 本地足够 |
| React / Next.js / Vue | 前端不是亮点，原生 HTML 足够展示功能 |
| LlamaIndex | 与 LangChain RAG 功能重叠，不引入第二套体系 |
| Celery / Redis | 任务队列过重，同步处理满足需求 |
| pytest | 可选后期加，不作为核心开发目标 |

---

## 三、项目目录结构

```
lunwen-agent/
├── 设计.md                    # 产品设计文档
├── 开发.md                    # 技术开发文档
├── requirements.txt           # Python 依赖
├── .env.example               # 环境变量示例（不含真实 key）
├── .env                       # 本地环境变量（不提交 Git）
├── .gitignore
├── main.py                    # FastAPI 入口，所有 endpoints
├── config.py                  # LLM 切换配置，session key 存储
│
├── agents/
│   └── research_agent.py      # AgentExecutor 主逻辑，组装 Tools + Memory
│
├── tools/                     # 8 个 LangChain Tool
│   ├── extract_tool.py        # 结构化提取（F2）
│   ├── summary_tool.py        # 快速摘要（F3）
│   ├── compare_tool.py        # 论文对比（F5）
│   ├── search_tool.py         # RAG 语义检索（F4 辅助）
│   ├── gap_tool.py            # 研究缺口（F6）
│   ├── citation_tool.py       # 参考文献提取
│   ├── arxiv_tool.py          # ArXiv 论文搜索
│   └── web_search_tool.py     # Tavily 网络搜索
│
├── memory/
│   └── session_memory.py      # ConversationBufferWindowMemory 封装（多用户隔离版）
│
├── rag/
│   ├── loader.py              # PyMuPDF PDF 解析
│   ├── splitter.py            # RecursiveCharacterTextSplitter 分块
│   └── vectorstore.py         # FAISS 索引管理
│
├── prompts/
│   ├── extract_prompt.py      # 结构化提取 prompt
│   ├── compare_prompt.py      # 对比分析 prompt
│   ├── gap_prompt.py          # 研究缺口 prompt
│   └── qa_prompt.py           # 多轮问答 prompt
│
├── schemas/
│   └── paper_schema.py        # Pydantic Paper 模型
│
└── frontend/
    ├── index.html             # 主页面
    └── app.js                 # 前端逻辑（API 调用）
```

---

## 四、API Endpoints

| Method | Path | 功能 |
|--------|------|------|
| POST | `/config` | 保存用户的 API Key（LLM + Tavily） |
| POST | `/upload` | 上传 PDF，解析并建立 FAISS 索引 |
| POST | `/extract` | 对指定论文执行结构化提取 |
| POST | `/summary` | 对指定论文生成摘要 |
| POST | `/chat` | 多轮对话问答（带 Memory） |
| POST | `/compare` | 对比多篇论文 |
| POST | `/gap` | 识别研究缺口 |
| POST | `/arxiv` | ArXiv 搜索 |

---

## 五、本地开发启动

```bash
# 1. 创建虚拟环境
python -m venv venv
venv\Scripts\activate        # Windows
# source venv/bin/activate   # Mac/Linux

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置环境变量（复制示例文件）
cp .env.example .env
# 然后编辑 .env 填入你的 key（可选，前端也可以填）

# 4. 启动服务
uvicorn main:app --reload --port 8000
```

---

## 六、部署（Railway + Cloudflare）

1. 推送代码到 GitHub
2. Railway 连接仓库，自动检测 Python，设置启动命令：`uvicorn main:app --host 0.0.0.0 --port $PORT`
3. Cloudflare 添加域名，DNS 指向 Railway 提供的地址
4. HTTPS 自动生效
