# 论文 Agent 项目学习路线

主线：按真实开发顺序，从 0 到 1 理解并复现这个项目。

每个模块都按这个结构学：

1. 为什么现在做这个
2. 这一阶段要写哪些代码
3. 会涉及哪些技术点
4. 你要学到什么
5. 怎么判断学会了
6. 如果使用 AI 辅助，应该怎么用、怎么说

## 模块 1：最小 FastAPI 服务

### 为什么现在做这个

任何工程第一步不是做复杂功能，而是先让服务能跑起来。

### 小节

1. 创建项目目录
2. 创建 `main.py`
3. 创建 FastAPI app
4. 写一个健康检查接口 `/`
5. 用 `uvicorn` 启动服务
6. 浏览器访问接口
7. 理解“服务启动成功”的意义

### 要学到什么

- FastAPI 最小结构
- `app = FastAPI()`
- `@app.get("/")`
- `uvicorn main:app --reload --port 8000`
- 后端服务是什么
- HTTP 请求和响应的最小流程

### 判断学会

你能解释：

> 为什么第一个版本只需要返回 `{"status": "ok"}`？

你能画出：

```text
浏览器访问 /
-> FastAPI 接收请求
-> 返回 JSON
```

### AI 辅助建议

可以让 AI 生成最小 FastAPI 示例，但你必须理解：

- `main:app` 是什么意思
- `@app.get("/")` 是什么意思
- 为什么要先做健康检查接口

## 模块 2：PDF 上传

### 为什么现在做这个

论文 Agent 的第一步是让用户把论文传进系统。

### 小节

1. 创建 `uploads/` 目录
2. 写 `/upload` 接口
3. 使用 `UploadFile`
4. 校验文件后缀是否是 `.pdf`
5. 保存文件到本地
6. 返回文件名和上传状态
7. 用前端或接口工具测试上传

### 要学到什么

- 文件上传
- `UploadFile`
- `File(...)`
- 后端保存文件
- 基础参数校验
- `HTTPException`

### 判断学会

你能解释：

> 为什么上传接口不能只相信前端传来的文件？

你能说出：

```text
用户上传 PDF -> 后端保存到 uploads/ -> 返回结果
```

### AI 辅助建议

可以让 AI 写上传接口初稿，但你要检查：

- 是否限制 PDF
- 是否处理文件名
- 是否保存到正确目录
- 上传失败时有没有错误返回

## 模块 3：PDF 解析

### 为什么现在做这个

保存 PDF 只是存文件，Agent 真正需要的是论文文本。

### 小节

1. 创建 `rag/loader.py`
2. 引入 PyMuPDF
3. 打开 PDF
4. 遍历每一页
5. 提取文本
6. 返回全文和页数
7. 在 `/upload` 中调用 `load_pdf`
8. 处理扫描版 PDF 无文本的情况

### 要学到什么

- PyMuPDF / `fitz`
- PDF 文本提取
- page count
- 文本为空的异常处理
- 文件处理和业务处理分离

### 判断学会

你能解释：

> 为什么 PDF 保存成功不代表论文解析成功？

你能说出：

```text
PDF 文件 -> load_pdf -> full_text + page_count
```

### AI 辅助建议

AI 可以帮你查 PyMuPDF 用法，但你需要自己判断：

- 文本为空怎么处理
- 是否要返回页数
- loader 为什么单独放在 `rag/loader.py`

## 模块 4：论文文本存储和 paper_id

### 为什么现在做这个

后续摘要、提取、问答都需要通过一个 ID 找到对应论文。

### 小节

1. 设计 `paper_id`
2. 用文件名生成 `paper_id`
3. 创建 `paper_texts` 字典
4. 上传后保存 `paper_texts[paper_id] = full_text`
5. 写 `GET /papers`
6. 写 `DELETE /papers/{paper_id}`
7. 理解内存存储的优缺点

### 要学到什么

- ID 设计
- 字典存储
- 内存状态
- GET / DELETE 接口
- 资源管理接口
- REST 风格基础

### 判断学会

你能解释：

> 为什么后续接口不直接传整篇论文，而是传 paper_id？

你能说出：

```text
paper_id 是论文在系统里的索引。
```

### AI 辅助建议

AI 可以帮你补接口，但你要检查：

- paper_id 是否稳定
- 删除论文时是否只影响当前存储
- 内存存储重启会丢失这个限制你是否知道

## 模块 5：LLM 配置管理

### 为什么现在做这个

在调用 LLM 前，必须先解决 API Key、模型、provider 怎么配置。

### 小节

1. 创建 `config.py`
2. 设计配置字段
3. 写 `POST /config`
4. 保存 API Key
5. 支持 provider / model / base_url
6. 写 `_require_api_key`
7. 理解为什么不能硬编码 key
8. 理解环境变量和运行时配置的区别

### 要学到什么

- 配置管理
- API Key 安全
- provider
- model
- base_url
- OpenAI-compatible API
- 后端状态管理

### 判断学会

你能解释：

> 为什么不能把 API Key 写死在代码里？

你能说出：

```text
/config 保存配置，后续 LLM 调用从配置中读取 key 和 model。
```

### AI 辅助建议

AI 可以帮你写配置类，但你要检查：

- key 是否会被提交到仓库
- 不同 provider 参数是否不同
- 未配置 key 时是否能及时报错

## 模块 6：第一个 LLM 功能：摘要

### 为什么现在做这个

这是项目第一条完整 AI 链路，先不要做 Agent，先把最小 LLM 功能跑通。

### 小节

1. 创建 `tools/summary_tool.py`
2. 从 `paper_texts` 读取论文文本
3. 构造摘要 prompt
4. 调用 LLM
5. 写 `/summary`
6. 检查 paper_id 是否存在
7. 检查 API Key 是否存在
8. 返回摘要结果

### 要学到什么

- LLM 调用流程
- Prompt 基础
- Tool 初步封装
- endpoint 调用业务函数
- paper_id 到文本的映射

### 判断学会

你能解释：

> 为什么摘要功能是第一个 LLM 功能？

你能画出：

```text
/summary
-> paper_id
-> paper_texts[paper_id]
-> prompt
-> LLM
-> summary
```

### AI 辅助建议

AI 可以帮你写 prompt 初稿，但你要判断：

- 输入文本是否过长
- 输出是否符合用户需求
- 错误时是否能返回明确原因

## 模块 7：结构化提取

### 为什么现在做这个

摘要是自由文本，但面试和真实应用更看重可控输出，所以要做结构化提取。

### 小节

1. 创建 `schemas/paper_schema.py`
2. 定义论文字段
3. 创建 `prompts/extract_prompt.py`
4. 使用 PydanticOutputParser
5. 创建 `tools/extract_tool.py`
6. 写 `/extract`
7. 处理 JSON 解析失败
8. 对比摘要和结构化提取

### 要学到什么

- Pydantic
- schema
- 结构化输出
- OutputParser
- JSON 格式约束
- Prompt 约束

### 判断学会

你能解释：

> 为什么结构化提取不能只靠普通 prompt？

你能说出：

```text
Pydantic schema 像一张表，要求模型按固定字段填内容。
```

### AI 辅助建议

AI 可以帮你设计字段，但你要判断：

- 字段是否符合论文阅读场景
- 输出是否可解析
- 失败时是否有兜底策略

## 模块 8：文本切分

### 为什么现在做这个

当论文太长时，不能直接把全文放进 prompt，需要为 RAG 做准备。

### 小节

1. 创建 `rag/splitter.py`
2. 选择文本切分器
3. 设置 chunk size
4. 设置 overlap
5. 为每个 chunk 添加 metadata
6. 在 `/upload` 中调用 split
7. 查看 chunk 数量
8. 理解 chunk 对检索质量的影响

### 要学到什么

- chunk
- overlap
- token 限制
- metadata
- RecursiveCharacterTextSplitter
- 长文本处理

### 判断学会

你能解释：

> chunk 太大和太小分别有什么问题？

你能说出：

```text
切分是为了让后续检索能找到最相关的局部内容。
```

### AI 辅助建议

AI 可以帮你查切分器参数，但你要判断：

- chunk size 是否适合论文
- overlap 是否必要
- metadata 是否保留了 paper_id

## 模块 9：FAISS 语义检索

### 为什么现在做这个

有了 chunk，还需要一种方式按“意思”找相关片段。

### 小节

1. 创建 `rag/vectorstore.py`
2. 理解 embedding
3. 初始化 embedding 模型
4. 创建 FAISS 索引
5. 增量添加文档
6. 写 search 函数
7. 根据 paper_id 过滤结果
8. 创建 `tools/search_tool.py`

### 要学到什么

- embedding
- vector search
- FAISS
- similarity search
- metadata filter
- 向量索引
- 检索结果排序

### 判断学会

你能解释：

> FAISS 和关键词搜索有什么不同？

你能说出：

```text
FAISS 存的是文本 chunk 的向量，用来按语义相似度找相关内容。
```

### AI 辅助建议

AI 可以帮你写 FAISS 调用代码，但你要检查：

- embedding 模型是否配置正确
- 检索结果是否带 metadata
- 是否按 paper_id 限制范围

## 模块 10：RAG 问答

### 为什么现在做这个

现在已经能检索论文片段，可以让 LLM 基于片段回答问题。

### 小节

1. 理解 RAG 流程
2. 用户问题进入 `/chat`
3. 检索相关 chunk
4. 拼接 context
5. 构造回答 prompt
6. 调用 LLM
7. 返回答案
8. RAG 和直接问 LLM 的区别

### 要学到什么

- Retrieval Augmented Generation
- context
- grounding
- 幻觉控制
- 问答链路
- prompt 拼接

### 判断学会

你能解释：

> RAG 为什么能减少幻觉？

你能画出：

```text
问题
-> FAISS 检索相关片段
-> 片段 + 问题给 LLM
-> 基于论文回答
```

### AI 辅助建议

AI 可以帮你写 RAG prompt，但你要检查：

- 是否要求模型基于材料回答
- context 是否太长
- 无相关片段时怎么处理

## 模块 11：多论文对比和 gap 分析

### 为什么现在做这个

单篇论文能力跑通后，再扩展到多篇论文分析，这是论文研究场景的核心价值。

### 小节

1. 设计多论文输入
2. 创建 `compare_tool`
3. 创建 `gap_tool`
4. 写 `/compare`
5. 写 `/gap`
6. 校验至少两篇论文
7. 设计对比维度
8. 设计 gap 输出格式

### 要学到什么

- 多输入处理
- Prompt 设计
- Markdown 表格
- 研究缺口分析
- 参数校验
- LLM 综合分析

### 判断学会

你能解释：

> 为什么 compare 和 gap 至少需要两篇论文？

你能说出：

```text
compare 偏横向对比，gap 偏发现共同未解决的问题。
```

### AI 辅助建议

AI 可以帮你优化对比维度，但你要判断：

- 输出是否符合科研阅读场景
- 是否引用具体论文
- 是否避免空泛结论

## 模块 12：LangChain Tools 标准化

### 为什么现在做这个

当能力越来越多，需要统一封装成 Agent 可以调用的工具。

### 小节

1. 什么是 Tool
2. 为什么普通函数不够
3. 给 summary / extract / search / compare / gap 加 `@tool`
4. 统一输入输出
5. 写工具描述
6. 让工具能被 Agent 识别
7. 工具边界设计
8. 工具和 endpoint 的关系

### 要学到什么

- LangChain Tool
- `@tool`
- 工具描述
- 单一职责
- Agent 可调用接口
- 模块化设计

### 判断学会

你能解释：

> 为什么有了 endpoint，还要有 Tool？

你能说出：

```text
endpoint 是给前端调用的，Tool 是给 Agent 调用的。
```

### AI 辅助建议

AI 可以帮你加 `@tool` 模板，但你要判断：

- 工具描述是否清晰
- 输入格式是否简单
- 每个工具是否只做一件事

## 模块 13：Agent

### 为什么现在做这个

只有当系统已经有多个工具时，Agent 才有存在价值。

### 小节

1. Agent 是什么
2. ReAct 是什么
3. 创建 `agents/research_agent.py`
4. 注册工具列表
5. 创建 AgentExecutor
6. 收集工具调用步骤
7. `run_agent`
8. 为什么不让所有请求都走 Agent

### 要学到什么

- Agent
- ReAct
- AgentExecutor
- tool selection
- callback
- latency / cost
- 工程取舍

### 判断学会

你能解释：

> 为什么 Agent 不应该是项目第一步？

你能说出：

```text
Agent 适合复杂问题，但普通固定任务直接调用 Tool 更快、更可控。
```

### AI 辅助建议

AI 可以帮你写 Agent 初始化代码，但你要判断：

- 工具列表是否合理
- prompt 是否包含工具说明
- Agent 是否增加了不必要成本

## 模块 14：Memory

### 为什么现在做这个

单轮问答完成后，需要支持连续追问。

### 小节

1. 多轮对话为什么需要 Memory
2. 创建 `memory/session_memory.py`
3. 使用 ConversationBufferWindowMemory
4. 设置 `k=10`
5. 在 chat 中读取历史
6. 重置 memory
7. Memory 和 RAG context 的区别
8. Memory 太长的问题

### 要学到什么

- Conversation memory
- chat history
- window memory
- 上下文长度
- 多轮追问
- reset

### 判断学会

你能解释：

> Memory 和 RAG 检索出来的 context 有什么区别？

你能说出：

```text
Memory 记的是对话历史，RAG 查的是论文内容。
```

### AI 辅助建议

AI 可以帮你接入 Memory，但你要判断：

- 记几轮合适
- 什么时候清空
- 是否会污染新对话

## 模块 15：多会话隔离

### 为什么现在做这个

前面是单用户 demo，真实应用需要防止不同用户数据串扰。

### 小节

1. 单全局变量的问题
2. `X-Session-ID`
3. `ContextVar`
4. session_config
5. `_all_paper_texts`
6. `_all_memories`
7. middleware 如何设置当前 session
8. 当前方案的局限

### 要学到什么

- session
- middleware
- ContextVar
- 全局状态隔离
- 多用户并发
- 内存存储
- 安全边界

### 判断学会

你能解释：

> 为什么全局 paper_texts 会导致多用户串数据？

你能说出：

```text
session_id 决定当前请求访问哪一份配置、论文和记忆。
```

### AI 辅助建议

AI 可以帮你改代码结构，但你要重点检查：

- 所有全局状态是否都隔离
- 每次请求是否正确设置 session
- 异常后是否 reset ContextVar

## 模块 16：前端演示界面

### 为什么现在做这个

后端能力需要一个可演示入口，否则面试展示不直观。

### 小节

1. 前端需要哪些功能
2. 配置面板
3. 上传论文
4. 论文列表
5. 摘要 / 提取按钮
6. 对话窗口
7. 对比 / gap 操作
8. 步骤可视化
9. localStorage 保存历史

### 要学到什么

- 基础前端交互
- fetch API
- JSON 请求
- 文件上传
- localStorage
- UI 和后端接口对应关系

### 判断学会

你能解释：

> 前端每个按钮对应哪个后端接口？

你能说出：

```text
前端不是核心算法，但它让项目成为可演示产品。
```

### AI 辅助建议

AI 可以帮你写 UI 初稿，但你要检查：

- 是否真的调用后端接口
- session_id 是否带上
- 错误信息是否能展示

## 模块 17：测试

### 为什么现在做这个

项目要写进简历，必须证明不是只能手动演示一次。

### 小节

1. 为什么写测试
2. API 测试
3. RAG pipeline 测试
4. session 隔离测试
5. 不依赖真实 LLM 的测试
6. pytest 运行方式
7. 测试覆盖了什么
8. 测试没覆盖什么

### 要学到什么

- pytest
- 单元测试
- API 测试
- 测试隔离
- mock 思想
- 回归测试
- 质量保障

### 判断学会

你能解释：

> 为什么测试不应该依赖真实 LLM？

你能说出：

```text
测试覆盖工程逻辑，真实 LLM 效果另用 eval 或人工评估。
```

### AI 辅助建议

AI 可以帮你写测试样例，但你要自己确认：

- 断言是否有意义
- 测试是否稳定
- 是否覆盖关键风险

## 模块 18：Docker 部署

### 为什么现在做这个

最后要让项目从“本机能跑”变成“别人也能跑”。

### 小节

1. Docker 是什么
2. Dockerfile
3. requirements.txt
4. `.dockerignore`
5. docker-compose
6. 端口映射
7. volume
8. 环境变量
9. 重启后状态变化

### 要学到什么

- Docker image
- container
- dependency install
- port mapping
- volume
- compose
- 部署边界

### 判断学会

你能解释：

> 为什么 Docker 不能自动解决所有持久化问题？

你能说出：

```text
上传文件可以用 volume 保留，但内存里的 session 和 FAISS 索引重启会丢。
```

### AI 辅助建议

AI 可以帮你写 Dockerfile，但你要检查：

- 依赖是否安装
- 启动命令是否正确
- 端口是否暴露
- 哪些文件不该复制进镜像

## 模块 19：README、简历和面试表达

### 为什么现在做这个

项目完成后，需要把工程过程讲成面试官能理解的故事。

### 小节

1. README 怎么写
2. 项目一句话介绍
3. 技术栈怎么写
4. 简历 bullet
5. 开发过程描述
6. 技术难点描述
7. 项目不足描述
8. AI 辅助开发描述
9. 模拟面试

### 要学到什么

- 项目表达
- 简历写法
- 面试叙事
- 技术取舍
- 项目复盘
- vibe coding 表达边界

### 判断学会

你能完整回答：

> 请介绍一下你的项目。

并能被追问：

```text
为什么用 RAG？
为什么用 Agent？
为什么这样做 session 隔离？
你怎么测试？
AI 在项目里帮了你什么？
```

### AI 辅助建议

AI 可以帮你润色表达，但你要保证：

- 每句话都能被代码支撑
- 不夸大项目能力
- 不把 AI 辅助说成 AI 代写

## 建议学习方式

### 窗口 A：学习窗口

我们在这里按模块讲：

```text
为什么做
-> 怎么做
-> 项目里怎么实现
-> 面试怎么说
```

### 窗口 B：从 0 搭建窗口

你自己或借助 AI 从空项目模仿搭建：

```text
每学完一个模块
-> 在新项目里实现一个最小版本
-> 跑通
-> 再进入下一个模块
```

这样你会同时获得：

- 理解现有项目
- 亲手搭建经验
- 面试时能讲开发过程
- 能合理说明 AI 辅助开发
